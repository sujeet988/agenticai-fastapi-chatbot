name: CI

on:
  push:
    branches: [ main, agent-hub ]
  pull_request:
    branches: [ main, agent-hub ]

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.11'
      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install pytest fastapi requests
      - name: Run tests
        run: |
          pytest -q
